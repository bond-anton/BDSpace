language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.8"
# command to install dependencies
install:
- pip install -r requirements.txt
# command to run tests
script:
- python setup.py build_ext --inplace &&
  nosetests -v --with-coverage --cover-package=BDSpace
# codecov support
before_install:
- pip install scipy Cython codecov
after_success:
- codecov